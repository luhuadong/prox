# Changelog

本项目遵循语义化版本。

## 0.1.0 - 2026-09-05

首个可用版本：

- Linux + Bash 4.3+；
- HTTP Proxy；
- `prox init bash`；
- `prox on` / `prox off`；
- `prox status`；
- `prox check`；
- `prox run -- <command>`；
- 环境快照、恢复和漂移检测；
- TCP 与 HTTP/HTTPS 分层健康检查；
- 严格 JSON 配置；
- 零第三方 Go Module。
