# Security Policy

## Supported versions

`prox` 当前处于 V0.x 阶段。安全修复只发布到最新版本。

## Reporting a vulnerability

请通过代码托管平台的私密安全报告功能联系维护者，不要先创建公开 Issue。

报告建议包含：

- 受影响版本；
- 可复现的最小步骤；
- 可能造成的影响；
- 建议的修复方式（如有）。

维护者应在公开仓库建立后，于本文件补充正式的安全联系地址。

## Security boundaries

`prox` 管理当前 Shell 和目标子进程的代理环境变量，不提供代理服务器，也不检查代理服务本身是否可信。

用户应只配置自己信任的代理端点。通过代理发送的元数据和未加密 HTTP 流量可能被代理运营方观察。
